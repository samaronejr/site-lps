# Migration reconciliation workbook — template

Fill one workbook per rehearsal. Sections 1-4 are generated by
`node scripts/migration/rehearsal.mjs reconcile --corpus=<package> --out=<file>`;
sections 5-7 are completed by the operator from the stage transcripts.

## 1. Run identity

| field | value |
| --- | --- |
| corpus id | `<corpusId>` |
| package | `<path>` |
| staging target | `<LPS_REHEARSAL_TARGET>` |
| operator | `<name>` |
| started / finished (UTC) | `<iso8601>` / `<iso8601>` |

## 2. Counts

| metric | value | must satisfy |
| --- | --- | --- |
| source records | | `source = create + update + unchanged + quarantined` |
| target records | | `target = create + update + unchanged` |
| create | | |
| update | | each has recorded provenance |
| unchanged | | |
| quarantined | | each has an explicit blocker |
| blocked by stop | | `0` for an accepted rehearsal |
| reconciled | | `yes` |

## 3. Locales (BCP47)

| tag | records |
| --- | --- |
| `pt-BR` | |
| `en` | |

## 4. Dispositions

| source id | locale | action | class | blocker |
| --- | --- | --- | --- | --- |
| | | | | |

## 5. Idempotency (stages 3 and 4)

| check | stage 3 | stage 4 | verdict |
| --- | --- | --- | --- |
| records written | | `0` expected | |
| duplicates created | | `0` expected | |
| export hash | | must equal stage 3 | |
| relation count | | must equal stage 3 | |
| asset count | | must equal stage 3 | |

## 6. Route manifest (stage 6)

| inventoried path | expected | observed | hops | verdict |
| --- | --- | --- | --- | --- |
| | `200` / `301` / `410` | | | |

## 7. Rollback

| check | evidence | verdict |
| --- | --- | --- |
| pre-import hashes captured | | |
| rollback executed | | |
| post-rollback hashes match pre-import | | |

## 8. Sign-off

| gate | verdict | evidence |
| --- | --- | --- |
| counts reconcile exactly | | |
| zero silent partial overwrite | | |
| every quarantine has an unresolved blocker | | |
| second apply performs zero writes | | |
| all routes resolve as contracted | | |
| rollback restores pre-import state | | |
